site:
  title: Stargate documentation
  url: /docs/stargate/
  start_page: stargate::index.adoc

content:
  sources:
    - url: .
      branches: HEAD

ui:
  bundle:
    url: https://github.com/stargate/antora-ui-stargate/releases/download/v0.3/ui-bundle.zip
  supplemental_files: ./supplemental-ui
  # override the default _ dir for assets, as Jekyll will delete them
  output_dir: assets

asciidoc:
  attributes:
    source-highlighter: highlightjs
    page-pagination: ''
  extensions:
  - ./lib/tabs-block.js
