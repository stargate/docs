query getOrder {
  order(checkout_id: 1) {
    checkout_id
    reader {
      name
      user_id
      email
      address {
        city
      }
    }
    books {
      title
      isbn
      author
    }
  }
}
