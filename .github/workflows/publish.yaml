name: Publish docs

on:
  push:
    tags:
    - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

env:
  SITE_DIR: 'stargate/stargate'
jobs:
  build_site:
    name: "Build site with Antora"
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout
<<<<<<< HEAD
        uses: actions/checkout@v3
=======
        uses: actions/checkout@v2

>>>>>>> main
      - name: "Render redoc"
        run: |
          npm install -g redoc-cli

<<<<<<< HEAD
          redoc-cli bundle api/StargateRESTv2.yaml --output docs-src/stargate-develop/modules/develop/attachments/restv2.html
          redoc-cli bundle api/StargateRESTv1.yaml --output docs-src/stargate-develop/modules/develop/attachments/restv1.html
          redoc-cli bundle api/StargateDocv2.yaml --output docs-src/stargate-develop/modules/develop/attachments/docv2.html
      - name: "Switch antora.yml"
        run: |
          FILE=antora.yml
          cd $GITHUB_WORKSPACE/docs-src/stargate-core
            if [  -f "$FILE" ]; then
            rm antora.yml;
            echo "antora.yml removed"
          fi
          ln -s antora-stargate.yml antora.yml
          cd $GITHUB_WORKSPACE/docs-src/stargate-develop
            if [  -f "$FILE" ]; then
            rm antora.yml;
            echo "antora.yml removed"
          fi          
          ln -s antora-stargate.yml antora.yml
          cd $GITHUB_WORKSPACE      
=======
          redoc-cli bundle api/StargateRESTv2.yaml --output modules/ROOT/attachments/restv2.html
          redoc-cli bundle api/StargateRESTv1.yaml --output modules/ROOT/attachments/restv1.html
          redoc-cli bundle api/StargateDocv2.yaml --output modules/ROOT/attachments/docv2.html

>>>>>>> main
      - name: "Generate site using antora site action"
        uses: riptano/antora-site-action@main
        with:
<<<<<<< HEAD
         antora_playbook: playbooks/site-publish-stargate.yaml
      - name: "List folder"
        run: |
          ls -ltra $GITHUB_WORKSPACE/build/$SITE_DIR
      - name: "Fix attachments"
        run: |
          sudo mv $GITHUB_WORKSPACE/build/$SITE_DIR/develop/_attachments $GITHUB_WORKSPACE/build/$SITE_DIR/develop/attachments
          sudo sed -i 's/_attachments/attachments/g' $GITHUB_WORKSPACE/build/$SITE_DIR/develop/api.html
          ls -ltra $GITHUB_WORKSPACE/build/$SITE_DIR
          grep attachment $GITHUB_WORKSPACE/build/$SITE_DIR/develop/api.html
=======
         antora_playbook: site-publish.yaml
         site_sources_path: ./

      - name: "List folder"
        run: |
          ls -ltra $GITHUB_WORKSPACE

      - name: "Fix attachments"
        run: |
          sudo mv $GITHUB_WORKSPACE/build/$SITE_DIR/stargate/1.0/_attachments $GITHUB_WORKSPACE/build/$SITE_DIR/stargate/1.0/attachments
          sudo sed -i 's/_attachments/attachments/g' $GITHUB_WORKSPACE/build/$SITE_DIR/stargate/1.0/api.html
          ls -ltra $GITHUB_WORKSPACE/build/$SITE_DIR/stargate/1.0
          grep attachment $GITHUB_WORKSPACE/build/$SITE_DIR/stargate/1.0/api.html

>>>>>>> main
      - name: "Upload generated site"
        uses: actions/upload-artifact@v3
        with:
          name: site
          path: "${{ github.workspace }}/build/${{ env.SITE_DIR }}"

  deploy_site:
    runs-on: [ubuntu-latest]
    needs: [build_site]
    name: "Deploy GitHub Pages"
    steps:
     - name: Setup Node.js for use with actions
<<<<<<< HEAD
       uses: actions/setup-node@v3
       with:
         version: 14
     - name: Checkout
       uses: actions/checkout@v3
=======
       uses: actions/setup-node@v2
       with:
         node-version: '16'
         cache: 'npm'

     - name: Checkout
       uses: actions/checkout@v2

>>>>>>> main
     - name: Download generated site
       uses: actions/download-artifact@v3
       with:
         name: site
         path: "${{ github.workspace }}/${{ env.SITE_DIR }}"
         
     - name: Deploy to GitHub Pages
       uses: peaceiris/actions-gh-pages@v3
       with:
          personal_token: "${{ secrets.PUBLISH_STARGATE_DOCS }}"
          external_repository: stargate/website
          publish_branch: 'gh-pages'
          publish_dir: "${{ env.SITE_DIR }}"
          destination_dir: docs
          disable_nojekyll: true
