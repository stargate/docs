=== Pull the Docker image

This image contains the Cassandra Query Language (CQL), REST, Document, GraphQL APIs,
and GraphQL Playground, along with an Apache Cassandra^(TM)^ 4.0 backend.

[source,bash,subs="attributes+"]
----
docker pull stargateio/stargate-4_0:{stargate-docker-tag-40}
----
